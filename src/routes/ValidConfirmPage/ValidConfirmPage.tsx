import * as React from 'react'

import ConfirmationToken from '../../components/ConfirmationToken'

class ValidConfirmPage extends React.Component {
    render() {
        return (
            <ConfirmationToken />
        )
    }
}

export default ValidConfirmPage
