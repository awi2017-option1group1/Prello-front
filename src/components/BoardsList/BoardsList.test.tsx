
// import * as React from 'react'
// import { shallow } from 'enzyme'

// import { Card, Button } from 'semantic-ui-react'
// import BoardsList from './BoardsList'
// import Spinner from '../common/Spinner'

// describe('<Board />', () => {
//     it('should display a Spinner while loading', () => {
//         const boardsList = shallow(
//             <BoardsList
//                 boards={[{id: 1, name: '', isPrivate: false}]}
//                 error={undefined}
//                 addBoard={() => {return}}
//             />
//         )
//         expect(boardsList.find(Spinner).length).toBe(1)
//         expect(boardsList.find(Card.Group).length).toBe(0)
//         expect(boardsList.find('#task-lists').length).toBe(0)
//     })

//     it('should display a BoardsList', () => {
//         const boardsList = shallow(
//             <BoardsList
//                 boards={[{id: 1, name: '', isPrivate: false}]}
//                 error={undefined}
//                 addBoard={() => {return}}
//             />
//         )
//         expect(boardsList.find(Spinner).length).toBe(0)
//         expect(boardsList.find(Card.Group).length).toBe(1)
//         expect(boardsList.find(Button).length).toBe(1)
//     })
// })

describe('', () => {
    it('temporary test', () => {
        expect(0).toBe(0)
    })
})
